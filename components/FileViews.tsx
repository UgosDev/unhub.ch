import React from 'react';

// DEPRECATO dalla v8.7.0
// Questo file non è più in uso. La logica attiva è stata consolidata in `components/ImageInput.tsx`.
// Questo file è conservato per riferimento storico e verrà rimosso in una futura pulizia.

// Le interfacce dei props sono mantenute per evitare di interrompere potenziali importazioni di tipi, sebbene nessun codice le utilizzi.
interface FileViewsProps {}

export const FileDropzone: React.FC<any> = () => null;
export const PendingFilesView: React.FC<any> = () => null;