{
  "name": "unhub-ch-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install && node replace.js"
  },
  "dependencies": {
    "replace-in-file": "^7.1.0"
  },
  "engines": {
    "node": "20"
  }
}
